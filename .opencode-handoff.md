# .opencode-handoff — E-Commerce Rock Merch & Roll

**Rama actual:** `refactor/catalogo`
**Última actualización:** 2026-07-17

## Reglas de ejecución
- **Commit por fase/inciso**: Al terminar cada fase o sub-inciso de fase, hacer commit
  con mensaje claro. Siempre pedir aprobación del usuario antes de pushear.
- **Actualizar este handoff** al finalizar cada fase, reflejando el nuevo estado.

## Estado final — Fases 1 a 8 completadas ✅

### Fase 1 — Auditoría + Setup de Tests ✅
- Auditoría: 8 hallazgos (3 🚨 ALTA, 2 ⚠️ MEDIA, 3 ℹ️ BAJA)
- Reporte: `docs/reports/auditor/auditoria-fase1-2026-07-16.md`
- Mapa de dependencias actualizado: `docs/architecture/dependency-map.md`
- 37 tests de caracterización nuevos (ProductCard, ProductGrid, Navigation)

### Fase 2 — Vista de Catálogo y Filtros ✅
- FilterSidebar con checkboxes de categoría + range de precio + drawer mobile
- useCatalog extendido: filterByCategories (array), filterByPrice
- Layout 2 columnas: sidebar izq + grid der, filtros reactivos en tiempo real
- 36 tests nuevos de filtros (productService, useCatalog, FilterSidebar)

### Fase 3 — Detalle de Producto y Flujo de Compra ✅
- ProductDetailPage: selector talle (M/L/XL/XXL), cantidad (±), skeleton loading
- Ruta `/product/:id` en router casero
- ProductCard clickable navega a detalle
- "Comprar ahora" agrega al carrito + redirige a checkout
- 45 tests nuevos (ProductDetailPage, ProductCard click, navigation)

### Fase 4 — Integración y Bug Fix ✅
- 17 tests E2E nuevos (product-detail, filters, full-flow) → 31 E2E
- Bug crítico: talle no se persistía en carrito → corregido
  - `tallesDisponibles: ["M","L","XL","XXL"]` en db.json (remeras/buzos)
  - `CartItem.talleSeleccionado` diferencia items por id + talle
  - CartModal/CartItemRow muestran el talle seleccionado

### Fase 5 — Refactor App.tsx ✅
- Extraídos 10 componentes inline (~600 líneas) a archivos individuales:
  - `src/services/router.ts` — navigate() y getPath()
  - `src/components/router/Router.tsx` — ruteo SPA
  - `src/components/router/ProductDetailRoute.tsx` — carga de producto + estados
  - `src/components/layout/Header.tsx` — navbar + modal logout (React, sin Bootstrap JS)
  - `src/components/home/HeroSection.tsx`, `BannerServices.tsx`, `BrandSection.tsx`
  - `src/components/layout/Footer.tsx`
  - `src/components/catalog/ProductsSection.tsx`
  - `src/components/layout/ShopPage.tsx`, `AppContent.tsx`
- App.tsx reducido de 633 → 12 líneas (solo providers)

### Fase 6 — Eliminar dependencias frágiles ✅
- jsPDF: CDN → `npm install jspdf`, import como módulo ES, eliminar `types/jspdf.d.ts`
- authService.ts: reemplazar `(window as unknown as Record).Config` por interfaz tipada
- Header.tsx: menú mobile con `useState` en vez de `document.getElementById`

### Fase 7 — Integrar ToastProvider + TODOs ✅
- `<ToastProvider>` envuelve `<AppContent>` en App.tsx
- CartModal: toast error si carrito vacío al hacer checkout
- CheckoutPage: toast error si tarjeta inválida
- Eliminado `localStorage.removeItem('carrito')` bypass (clearCart ya lo persiste)

### Fase 8 — Limpieza final ✅
- Eliminado código muerto en productSearch.ts (3 funciones no usadas)
- `messageCounter` global → `useRef` local en useConcierge.ts
- 16 warnings de `act()` eliminados en concierge.test.js (vi.useFakeTimers)

## Suite de tests total
- **Unitarios (Vitest):** 193 tests ✅ (11 archivos, 0 warnings de act)
- **E2E (Playwright):** 31 tests ✅ (7 archivos)

## Deuda técnica resuelta
Todos los hallazgos de la auditoría Fase 1 fueron corregidos en Fases 5-8:
1. ✅ `(window as any).bootstrap.Modal` → modal React en Header.tsx
2. ✅ jsPDF CDN → npm install jspdf
3. ✅ Componentes inline en App.tsx → 10 archivos separados
4. ✅ TODOs en CartModal + CheckoutPage → toasts integrados con ToastProvider
5. ✅ localStorage bypass en CheckoutPage → eliminado

## Commits en refactor/catalogo
```
928bf12 chore: Fase 8 — limpieza final
a010c51 feat: Fase 7 — integrar ToastProvider + TODOs
3ef93c7 feat: Fase 6 — eliminar dependencias frágiles
cd0b739 feat: Fase 5 — refactor App.tsx (extraer componentes)
b0a007b fix: bug crítico — talle persiste en carrito
50651dc feat: Fase 4.1 — 17 tests E2E
efd3b54 feat: Fase 3.3 — 45 tests detalle producto
7a3354b feat: Fase 3.2 — ruta /product/:id
2287f54 feat: Fase 3.1 — ProductDetailPage UI
ed7c15e feat: Fase 2.3 — 36 tests filtros
800b347 feat: Fase 2.2 — vista catálogo con filtros
cd04d10 feat: Fase 1 — auditoría + 37 tests
```

## Referencia rápida
- Reglas y roster de agentes/skills: `AGENTS.md`
- Arquitectura y contratos: `docs/architecture/`
- Reportes de auditoría: `docs/reports/auditor/`
