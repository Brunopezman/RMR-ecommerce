# .opencode-handoff — E-Commerce Rock Merch & Roll

**Rama actual:** `test/ui-ux-skills`
**Última actualización:** 2026-07-16

## En curso / pendiente
- Sin tareas abiertas — todos los hallazgos de la auditoría UI/UX corregidos y migración Bootstrap completada.

## Recién completado
- **R4** — Migración completa Bootstrap → Tailwind puro:
  - Eliminado CDN Bootstrap CSS/JS/Popper/Icons de `index.html`
  - Eliminadas páginas HTML legacy (`pages/checkout.html`, `pages/shop.html`)
  - App.tsx: navbar y `bootstrap.Modal` API reemplazados por LogoutConfirmModal React puro
  - LoginModal.tsx, CheckoutPage.tsx, CartModal.tsx, ProductCard.tsx, ProductGrid.tsx: ~100 clases Bootstrap → Tailwind
  - Creado `LogoutConfirmModal.tsx` (componente React con focus trapping)
  - Eliminado shim `.is-invalid` de `index.css`
  - Fix: `onShowToast` no propagado a `ChatBubble` (bug TS preexistente)
  - Build exitoso, 70/70 tests pasan
- **R17** — Contraste coral resuelto: `coral-dark` actualizado a `rgb(191,80,40)` (ratio 4.53:1 ✅). Aplicado en text/hover/bg con texto blanco. Mantenido coral original para rings, íconos sociales (texto negro) y ShoppingConcierge (superficie decorativa grande).
- **R16** — Border-radius normalizado a `rounded-lg` en todos los CTAs
- **R20** — ProductCard imagen responsive: `h-36` mobile, `h-48` desktop
- **UX-09** — CTA "Limpiar carrito" simplificado a text-link sin borde
- **UX-10** — Toast al agregar al carrito: hook `useToast`, componente `Toast`
- **R12** — Target size 44×44px: navbar + footer social icons
- Auditoría UI/UX + Accesibilidad completa (28 hallazgos identificados).
- Corrección de 15 hallazgos críticos y alta prioridad: skip link, focus trapping
  en modales, lang=es, aria-live, focus-visible, aria-label, headings, typos,
  loading state en checkout, feedback visual en carrito, y más.
- 7 nuevos skills agregados en `.opencode/skill/`: ui-ux-review, accessibility,
  tailwind-design-system, responsive-design, react-best-practices,
  performance-audit, ux-writing.

## Referencia rápida
- Reglas y roster de agentes/skills: `AGENTS.md`
- Arquitectura y contratos: `docs/architecture/`
- Reporte de auditoría UI/UX: `docs/reports/ui-ux-audit.md`
